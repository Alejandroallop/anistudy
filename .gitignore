# --- DEPENDENCIAS ---
node_modules/
# ¡OJO! NUNCA ignores package-lock.json, es vital para producción.
# package-lock.json  <-- ESTA LÍNEA LA HEMOS QUITADO A PROPÓSITO
yarn.lock
pnpm-lock.yaml

# --- VARIABLES DE ENTORNO (SECRETOS) ---
.env
.env.local
.env.*
backend/.env
frontend/.env

# --- LOGS Y SISTEMA OPERATIVO ---
logs/
*.log
npm-debug.log*
.DS_Store
Thumbs.db
desktop.ini

# --- IDEs (Editor) ---
.vscode/
.idea/
*.swp

# --- BUILD (Lo que se genera al compilar) ---
dist/
build/
*.tgz
coverage/